#!/usr/bin/env php
<?php

use Careminate\Console\Application;
use Careminate\Supports\Console\ColorOutput;

// Set base path
define('BASE_PATH', __DIR__);

require BASE_PATH . '/vendor/autoload.php';

ColorOutput::init();

$app = new Application();
$app->run($argv);